- hosts: docker
  connection: docker
  become: yes
  vars:
    - include: vars/server.yml
  roles:
    - git
    - go
    - mariadb
    - postfix
    - dovecot
    - vim
    - zsh
    - mackerel
    - others
