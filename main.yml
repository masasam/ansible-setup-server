- hosts: all
  user: ansible
  become: yes
  roles:
    - nginx
    - mariadb
    - postfix
    - vim
    - zsh
    - git
    - cyrus-sasl
    - dovecot
    - monit
    - nmap
    - certbot
    - systemd
  tasks:
    - name: install all
      pacman: name={{ item }} state=installed
      with_items:        
        - bash-completion
        - zsh-completions
        - tig
        - wget
        - htop
        - rsync
        - tmux
